#include<bits/stdc++.h>
using namespace std;
void backtrack(vector<int> &arr,int idx,vector<int> &temp, vector<vector<int>> &result){
    if(idx==arr.size()){
        if(temp.size()>=2)
         result.push_back(temp);
        return;
    }
    if(temp.size()>=2){
        
    }
}
int main(){
    
}